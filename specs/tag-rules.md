```markdown
Tag rules

1. <HTML> ... </HTML>
Create an HTML file from the enclosed content.

2. <worksheet> ... </worksheet>
Create a Word document from the enclosed content.

3. <document> ... </document>
Create a Word document from the enclosed content.

4. <filename> "name.ext" </filename>
Use this exact filename for the generated output.

5. Content outside recognised tags is reference or planning material and should not be turned into output files unless explicitly instructed.

6. If a block contains instructions rather than course content, classify it as specification text and do not publish it as learner-facing content.
```